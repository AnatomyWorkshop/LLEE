# LLEE 进化路径与严谨实验计划（v3）

> 2026-04-20
> 综合 Evolutionary-thinking.txt 的疑问与建议
> 本文档是最终执行计划，整合了所有前序思考

---

## 零、本文档的定位

| 文档 | 回答什么 |
|------|---------|
| LLEE-demo-predesign.md | 概念层：设计前需要想清楚什么？ |
| LLEE-experiment-plan.md | 执行层：想清楚之后怎么做？ |
| **本文档** | 修正层：前两份计划的隐性假设是否成立？进化路径如何融入？ |

---

## 一、进化路径的价值判断

### 核心洞察

Evolutionary-thinking.txt 揭示了一个关键事实：**当前实验计划的评估指标（CLIP 分数、填充率、证据校准度）本身就是进化算法的适应度函数。** 实验计划和自进化计划不是两件事——前者是后者的手动版本。

```
当前计划（人类驱动）：
  人类设计 Schema → 跑实验 → 看指标 → 人类修改 Schema → 再跑

自进化计划（算法驱动）：
  初始 Schema → 跑实验 → 计算适应度 → 遗传算法变异 Schema → 再跑

唯一的区别：谁在做"选择"这一步。
```

### 这意味着什么？

1. **不需要额外的"自进化实验"**——只需要在 Phase 2 之后，把人类的"看指标→改 Schema"替换为脚本的"读指标→变异 Schema"
2. **进化不是远期梦想，是当前实验的自然延伸**
3. **但 Demo 阶段不做进化**——先证明"人类设计的 Schema 有效"，再证明"Schema 可以自动优化"

### 联想到的其它内容

进化路径的思考还可以延伸到：

**A. Schema 的"物种分化"**

不同类型的叙事（动作戏 vs 对话戏 vs 环境描写）可能需要不同的 Schema 粒度。进化算法可以自然产生"物种分化"——一套 Schema 分裂为多个特化变体，每个变体适应不同的叙事生态位。

**B. 证据分级规则的进化**

当前的证据分级规则（TEXT 置信度上限 1.0，PREDICTED 上限 0.4）是人工设定的。这些阈值本身也可以被进化搜索优化——什么样的阈值组合能最大化人类评估者的"忠实度"评分？

**C. 与 JEPA 的真正交汇点**

JEPA 学习连续表征，LLEE 进化离散符号。两者的交汇点是：**用 JEPA 学到的表征空间作为 LLEE 进化的"地形图"**——符号在表征空间中的位置决定了它们的语义距离，进化算法在这个地形上搜索最优的符号分布。这是"逆 LLEE"方向的一个具体化。

---

## 二、回应五个疑问——修正实验设计

### 疑问 1 回应：语料差异是否足够大？

**这是最关键的前置验证。必须在正式实验前确认。**

```
解决方案：预实验（Pre-study）

Step 1：从两个语料中各取 5 个段落，确认它们描述的是同一叙事事件
  - 如果不是 → 需要人工创建对齐语料（同一事件的精简/丰富两种写法）
  - 如果是 → 继续

Step 2：量化信息密度差异
  - 词数比：丰富版 / 精简版 的平均词数比（目标 > 2.0）
  - 感官词密度：每 100 词中涉及视觉/听觉/触觉/嗅觉的词数
  - 实体提及密度：每 100 词中提及的可渲染实体数量
  
  如果这些指标差异不显著 → 语料不合格，需要重新准备

Step 3：用 3 个段落试跑 Parser，检查填充率差异
  - 如果丰富版填充率 < 精简版的 1.5 倍 → 差异不够，需要加大语料差距
```

**对计划的修改**：在 Phase 0 中增加"语料验证"步骤，作为 Phase 1 的门控条件。

### 疑问 2 回应：CLIP 分数对精简语料是否公平？

**确实不公平。CLIP 天然偏好信息密集的文本。**

```
解决方案：双度量体系

度量 A — CLIP 对齐分数（自动化，有偏但可复现）
  用途：大规模自动评估，作为筛选工具
  局限：对精简语料不公平，H2 可能是平凡的

度量 B — 归一化忠实度分数（半自动，公平但昂贵）
  定义：渲染结果中"正确呈现的信息量" / "源文本中可渲染的信息总量"
  
  例：
    精简文本："阿拉丁走进洞穴"
    可渲染信息：{人物=阿拉丁, 动作=走, 目标=洞穴} = 3 个信息单元
    渲染结果正确呈现了 3 个 → 归一化忠实度 = 3/3 = 1.0
    
    丰富文本："阿拉丁弯腰穿过低矮石缝，潮湿空气扑面，远处铜灯发蓝光"
    可渲染信息：{人物, 姿态=弯腰, 空间=低矮石缝, 触觉=潮湿, 光源=铜灯, 光色=蓝} = 6 个
    渲染结果正确呈现了 4 个 → 归一化忠实度 = 4/6 = 0.67

  这个度量对两种语料都公平：精简语料可以得满分（因为分母小），
  丰富语料更难得满分（因为分母大）。

度量 C — 人类直接评分（Phase 3，最终裁判）
  作为 CLIP 和归一化忠实度的校准基准
```

**对计划的修改**：
- Phase 1 增加"信息单元标注"——为每个段落标注可渲染信息单元数量
- Phase 2 同时报告 CLIP 分数和归一化忠实度
- 论文中讨论 CLIP 的偏差，用归一化忠实度作为补充

### 疑问 3 回应：WSM 状态漂移率的 ground truth

**接受建议：降级为定性观察。**

```
修改后的方案：

定量指标（保留但简化）：
  - 每 10 个段落设置一个"检查点"
  - 只在检查点处标注完整的累积状态（而非每段都标注）
  - 50 段序列 = 5 个检查点 = 5 次人工标注
  - 漂移率 = 检查点处 WSM 状态与人工标注的字段级不一致比例

定性观察（新增）：
  - 在 UI 中实时显示 WSM 当前状态
  - 人工抽查 3 个"叙事转折点"（场景切换、时间跳跃、新角色出场）
  - 记录观察笔记：WSM 是否正确处理了状态重置？
```

### 疑问 4 回应：Three.js 渲染质量是否足够？

**接受建议：聚焦环境而非角色。**

```
修改后的渲染策略：

Phase 2 的 Three.js 渲染聚焦于：
  ✓ 光照（方向、色温、强度、阴影）— 差异最明显
  ✓ 色调/氛围（后处理：色彩分级、雾效、对比度）— 情绪传达的主要载体
  ✓ 空间（场景几何：洞穴/沙漠/室内）— 环境类型的直接体现
  ✓ 粒子效果（灰尘、水汽、火星）— 增加沉浸感
  
  ✗ 角色面部表情 — Three.js 做不好，且不是核心验证点
  ✗ 角色动画 — 同上
  ✗ 精细材质 — PBR 参数差异在低多边形模型上不明显

角色处理方案：
  - 用剪影/轮廓表示角色位置和姿态
  - 或用 2D 立绘叠加在 3D 环境上（视觉小说的经典做法）
  - 情绪通过环境光照和音频传达，而非面部表情

增加 Phase 2.5 — 渲染质量感知测试（n=5）：
  "你觉得这个画面的质量是否足以让你判断它是否符合文本描述？"
  如果 < 4/5 人说"是" → 调整渲染策略后再进入 Phase 3
```

### 疑问 5（隐性假设）回应：LLM 结构化输出的稳定性

**这是最大的风险。必须在一切之前验证。**

```
新增 Phase 0.5 — Prompt 稳定性验证

目标：确认 Claude API + JSON Schema 约束能稳定输出合规的 LLEE Delta

方法：
  1. 选 5 个代表性段落（覆盖所有语言类型）
  2. 每个段落跑 20 次（共 100 次调用）
  3. 记录：
     - Schema 合规率（目标：100%）
     - 证据分级一致性（同一段落 20 次输出的证据等级是否稳定）
     - 情绪标签一致性（同一段落 20 次输出的情绪标签是否稳定）
  
  通过标准：
    - Schema 合规率 = 100%（结构化输出约束应保证这一点）
    - 证据分级一致性 > 80%（允许 CONTEXTUAL/UNDEFINED 边界有波动）
    - 情绪标签一致性 > 90%（核心判断应稳定）
  
  如果不通过：
    - 迭代 prompt 直到通过
    - 如果 10 轮迭代仍不通过 → 考虑换 LLM 或降低 Schema 复杂度
    - 记录所有迭代过程 → 论文素材

  时间预算：3-5 天（在 Phase 0 和 Phase 1 之间）
```

---

## 三、整合五个补充建议

### 补充 1：Schema 粒度消融实验 → 纳入

```
新增基线：
  Baseline C — LLEE Schema + 证据分级只有 HAS_EVIDENCE / NO_EVIDENCE 两档
  Baseline D — LLEE Schema + 材质枚举只有 3 个粗粒度值

在 Phase 1 的自动化评估中多跑两个变体，工作量增加约 20%
论文中作为 Ablation Study 呈现

这直接回答审稿人可能的问题：
  "四级证据分级真的比二元更好吗？"
  "枚举粒度对渲染保真度有多大影响？"
```

### 补充 2：IEI 泄漏细粒度分析 → 纳入

```
在 Phase 2 完成后，对 IEI 泄漏案例做逐例追溯：
  - 选 5 个泄漏最严重的段落
  - 追溯泄漏源头：Parser 输出了错误标签？还是渲染适配器过度表现？
  - 分类泄漏类型：情绪标签泄漏 / 证据等级泄漏 / 渲染强度泄漏
  
论文中作为 Error Analysis 呈现
```

### 补充 3：跨模态一致性可视化案例 → 纳入

```
Phase 2 完成后录制 15 秒屏幕录像：
  左：LLEE Delta 文本（高亮变化字段）
  中：Three.js 场景切换（沙漠→洞穴，光照变暗）
  右：音频切换（开阔→回声）
  
放在项目网页 / 论文 supplementary material
```

### 补充 4：与 ChatUSD 的定性对比 → 纳入

```
论文 Related Work 中增加一段：

ChatUSD (NVIDIA, GTC 2025)：
  - 交互式对话工具，面向人类艺术家
  - 逐轮对话调整场景，无状态一致性保证
  - 不处理情绪/证据分级问题

LLEE：
  - 管道式自动前端，面向叙事渲染自动化
  - 维护世界状态机，保证跨段落一致性
  - 核心关注 IEI 抑制和证据分级

两者互补而非竞争：ChatUSD 适合人类主导的场景编辑，
LLEE 适合大规模叙事内容的自动化渲染管道。
```

### 补充 5：最小可行 Schema → 已在 v2 计划中体现

Phase 0 只实现 E + V + S，O/H/T/N 用占位符。确认。

---

## 四、修正后的完整任务清单

### Phase 0 — 地基 + 语料验证（Week 1-2）

```
任务 0.1  语料验证（门控条件）
  □ 确认两个语料版本覆盖相同叙事事件序列
  □ 如果不对齐 → 人工创建对齐语料（同一事件的精简/丰富两种写法）
  □ 量化信息密度差异：词数比、感官词密度、实体提及密度、信息单元比
  □ 通过标准（满足任一即可）：
    - 信息单元比 > 1.5（基于 Phase 0.2 的信息单元标注）
    - 词数比 > 2.0 且感官词密度 > 1.5 倍
  □ 如果不通过 → 重新准备语料，不进入 Phase 1

任务 0.2  语料标注
  □ 选择 40 个对齐段落对
  □ 标注 30 个中性段落（IEI 测试用）
  □ 为每个段落标注：
    - 期望的世界状态（ground truth，JSON 格式）
    - 可渲染信息单元数量（归一化忠实度的分母）
    - 语言类型分布（描写/对话/命令/修辞/旁白 的比例）
    - expected_env_features: list[str]（文本中应被提取的环境特征）
      用于计算环境召回率 = |expected ∩ extracted| / |expected|
  □ 在 50 段连续序列中设置 5 个状态检查点

任务 0.3  Schema 定义
  □ Pydantic v2 定义 E + V + S 三组（完整实现）
  □ O + H + T + N 四组（占位符，optional 字段）
  □ Delta 格式（jsonpatch）
  □ 证据分级扩展枚举（5 来源 × 4 强度）
  □ 标签→参数映射表
  □ 消融变体 Schema：
    - Variant C：证据分级二元化
    - Variant D：材质枚举粗粒度化
  □ VisualField 增加 scene_features: list[str]（环境特征标签）
    - 文本中明确提及的环境特征（stalactite, dripping_water, fissure_light 等）
    - 渲染器通过确定性映射激活对应资产模块（标签→资产，非联想）
  □ WSM 置信度上限强制执行（_clamp_evidence，已实现）

任务 0.4  技术选型锁定
  □ LLM：Claude API（主力）+ Llama-3 本地（对比）
  □ 渲染：Three.js（环境为主，角色用剪影/立绘）
  □ 音频：Tone.js + Web Audio API
  □ 评估：OpenCLIP ViT-L/14
  □ 前端：React + Vite + WebSocket
```

### Phase 0.5 — Prompt 稳定性验证（Week 2-3，3-5 天）

```
任务 0.5.1  Prompt 工程
  ✅ 写系统 prompt 模板 v1（已完成）
  ✅ 写约束注入模板（已完成）
  ✅ 写上下文摘要模板（已完成）
  ✅ 增加环境提取规则 Critical Rule #6（已完成）
  ✅ Prompt v2 迭代：增加 Rule 7-11（意图≠情绪、抽象文本规则、
    标准情绪词表 15 标签、reverb/stage 枚举约束）

任务 0.5.2  稳定性测试
  ✅ 多模型 smoke test（GLM/DeepSeek Chat/Claude Opus 各 10/12 通过）
  ✅ GLM-4.5-air 5 轮一致性测试 × 3 轮迭代（共 180 次 API 调用）
  □ 测量结果（Prompt v2, GLM-4.5-air, 5 runs × 12 segments）：
    - Schema 合规率：平均 95%（目标 100%，剩余为 position 格式偶发）
    - 证据分级一致性：平均 93%（目标 >80%，✅ 达标）
    - 情绪标签一致性：平均 80%（目标 >90%，未达标）
    - 4/12 段落完全通过（aladdin_cave, masque_rooms, cthulhu, sakakibara_rich）
  □ 情绪一致性瓶颈分析：
    - 60-70% 段落（usher, sakakibara_sparse, karl_hallway）：模型固有噪声
    - 80-85% 段落（cask, karl_panic）：接近阈值，prompt 迭代有效
    - 哲学/抽象文本（cthulhu）：v2 prompt 修复后从 62%→100%
  □ 决策：以当前状态进入 Phase 1，记录所有迭代版本作为论文素材
    Phase 0.5 的价值在于暴露挑战性文本类型，不在于 100% 通过率
  □ 新增测量：环境填充率（非 None 环境字段数 / 总环境字段数）
    - 目标：丰富语料（Poe）> 60%，稀疏语料（Aladdin）> 20%
  □ 通过标准（修正）：
    - 合规 ≥ 95%（允许偶发格式错误）
    - 证据一致 > 80%（✅ 已达标）
    - 情绪一致 > 80%（从 90% 下调，反映实际可达水平）
    - 或：8/12 段落完全通过
  □ 如果不通过 → 继续迭代 prompt，记录迭代过程
  □ 最多 10 轮迭代，当前已完成 2 轮（v1→v2）

任务 0.5.3  记录
  ✅ 保存所有 prompt 版本和对应的稳定性数据（D:\LLEE\tasks\）
  ✅ 多模型对比数据（GLM/DeepSeek/Claude smoke test）
  ✅ Normalizer 演进记录（论文素材：LLM 结构化输出的常见类型错误）
```

### Phase 1 — Parser 管道 + 保真度实验（Week 3-5）

```
任务 1.1  WSM 实现
  □ World State Machine（~200 行 Python）
  □ 上下文摘要缓冲区（最近 5 段）
  □ 单元测试

任务 1.2  Parser 保真度实验
  □ 跑全部段落通过 5 个条件：
    - Baseline A：无约束 LLM 自由文本
    - Baseline B：JSON Schema 无证据分级
    - Baseline C：LLEE + 证据二元化（消融）
    - Baseline D：LLEE + 粗粒度枚举（消融）
    - LLEE Full：完整管道
  □ 计算指标：
    - 属性填充率（按语料类型分组）
    - IEI 泄漏率（中性段落）
    - 证据校准度（Cohen's Kappa vs 人工标注）
    - Schema 合规率
    - 环境填充率（按语料类型分组）
    - "虚假 UNDEFINED"比例（Parser 遗漏了文本中实际存在的环境信息）
  □ 双语料对比：精简 vs 丰富的填充率差异
  □ 消融对比：四级证据 vs 二元证据的校准度差异
  □ 对比有/无环境提取规则的 prompt 版本，量化 attention 盲区对策效果
  □ 生成图表和统计检验结果

  ⚡ 里程碑 1：Parser 保真度数据可独立发表
```

### Phase 2 — 渲染 + 自动化评估（Week 5-8）

```
任务 2.1  Three.js 渲染适配器
  □ LLEE Delta → Three.js 场景更新
  □ 聚焦：光照、色调、雾效、空间几何、粒子
  □ 角色：剪影/立绘方案
  □ WebSocket 实时推送

任务 2.2  音频适配器
  □ LLEE Delta → Tone.js / Web Audio 参数
  □ 环境音 + 混响 + 配乐张力
  □ 与视觉同步

任务 2.3  对比可视化 UI
  □ 左右分屏（精简 vs 丰富）
  □ LLEE Delta 实时显示
  □ 证据来源可视化
  □ 指标仪表盘

任务 2.4  渲染保真度实验
  □ 80 段渲染截图
  □ CLIP 对齐分数（所有条件）
  □ 归一化忠实度分数（基于信息单元标注）
  □ 跨模态一致性检测
  □ 50 段连续序列 + 5 个检查点的状态漂移观察
  □ IEI 泄漏逐例分析（5 个最严重案例）

任务 2.5  渲染质量感知测试（n=5）
  □ "画面质量是否足以判断文本忠实度？"
  □ 如果 < 4/5 通过 → 调整渲染策略
  
任务 2.6  跨模态一致性录像
  □ 录制 15 秒演示视频
  □ 场景切换 + 光照变化 + 音频同步

  ⚡ 里程碑 2：完整技术评估 + 演示视频
```

### Phase 3 — 人类评估 + 论文（Week 8-11）

```
任务 3.1  用户研究
  □ 问卷设计（A/B 偏好 + Likert 量表 + 开放反馈）
  □ 刺激材料准备（4 条件 × 10 段落 = 40 个渲染）
  □ 试点测试（n=5）
  □ 正式执行（n=30-50）
  □ 统计分析
  □ 认知相关性实验（新增）：
    - 被试在阅读过程中标注"当前场景氛围"（5 点量表）
    - 采用分段回顾标注：每读完 3-5 段，回看并标注氛围变化轨迹
      （避免实时标注打断阅读流，保持生态效度）
    - 与 WSM 的 narrative.tension 轨迹做 Pearson 相关分析
    - 目标 r > 0.7 → LLEE 衰减模型的认知验证
    - 这比 CLIP 分数更有说服力（直接测量人类感知与系统状态的对齐）

任务 3.2  论文撰写
  □ 目标会议选择
  □ 结构：问题→架构→实验1(Parser)→实验2(渲染)→实验3(人类)→双语料分析→消融→讨论
  □ Related Work 含 ChatUSD 对比
  □ Error Analysis 含 IEI 泄漏追溯
  □ Supplementary 含跨模态录像
  □ Discussion 新增：
    - LLM attention 偏好与世界状态完整性的张力
    - LLEE 置信度系统与 token 概率的数学同构（6 种证据 ≈ 6 个手工 attention head）
    - 符号系统与神经网络的互补压缩策略
    - "事实陈述原则"：LLEE 只建模文本事实，不建模角色认知
    - 本体论平等原则：LLEE 的置信度衡量"文本证据强度"，不衡量"物理真实性"
      架空世界和真实世界在 LLEE 框架内享有平等的本体论地位
    - 多世界扩展框架（WorldStack + EvidenceBridge），用 Karl Vision A/B/C 作为案例
      连接可能世界语义学（Marie-Laure Ryan）和心理叙事学
  □ Limitations 新增：
    - 当前系统将叙述者默认为可信（置信度 1.0）；未来可通过为每个声明
      绑定叙述者身份和可信度权重（source: narrator_x, reliability: 0.3）
      来支持不可靠叙述——这是证据分级系统的自然扩展方向
    - 环境信息提取受 LLM attention 分布限制（"虚假 UNDEFINED"问题）
    - 衰减率未经人类认知数据校准（Phase 3 认知实验为初步验证）
    - 当前为单世界架构，不支持嵌套叙事的多层世界状态
  □ Future Work 新增：
    - 空间骨架（@REGION）与环境特征系统
    - render_intensity 可微化（sigmoid 叠加替代分段线性）
    - 属性间竞争（情绪 softmax）
    - 贝叶斯更新替代 clamp
    - 不可靠叙述支持
    - 多世界本体论（WorldStack）：嵌套叙事、角色幻象、回忆、梦境
      世界间证据交换：主世界→子世界（CONTEXT 继承），子世界→主世界（PREDICTED 反馈）
    - CLIP 评估偏差的修正：CLIP 对架空内容的评分偏低（训练数据偏差），
      需要在评估中用归一化忠实度作为主要指标

  ⚡ 里程碑 3：可投稿论文
```

### Phase 4 — 进化实验（Week 12+ 可选）

```
任务 4.1  嵌套优化（Bi-level Optimization）
  □ 外层：进化算法搜索离散结构（Schema 拓扑）
    - 种群：20 个 Schema 变体（不同的证据类型组合、枚举集合）
    - 变异操作：增删证据类型、拆分/合并枚举、调整字段存在性
    - 多样性维护：计算种群 Jaccard 相似度，> 0.8 时强制移民
      （随机生成新 Schema 替换最相似个体，防止早熟收敛）
    - 选择：锦标赛选择，保留 top 5
  □ 内层：梯度下降优化连续参数
    - 对每个 Schema 变体，用梯度下降找到最优的：
      衰减率、置信度上限、source_weight、render_intensity 映射曲线
    - 损失函数：人类评分（Phase 3 数据）+ CLIP + 归一化忠实度（加权）
    - 每个变体在"最优参数"下被评估，确保公平比较
  □ 适应度 = 内层优化后的最佳损失值
  □ 跑 50-100 代
  □ 观察：Schema 是否自然演化出更细粒度的枚举？连续参数是否收敛？
  □ 提取 LLM 处理同一段文本时的 attention 权重，与优化后的衰减曲线对比
    - 如果形状相似 → 人工设计的衰减率接近最优
    - 如果差异大 → 嵌套优化找到了人类未预见的模式

任务 4.2  进化结果分析
  □ 进化后的 Schema vs 人类设计的 Schema：哪个适应度更高？
  □ 进化产生了哪些人类未曾想到的枚举细分？
  □ 这是"自进化世界语"的第一个实证数据点

任务 4.3  扩展渲染器
  □ Omniverse Kit SDK 集成（跳过 UE5）
  □ FMOD 集成
  □ 更多语料（其它公版文学）

任务 4.4  可微化与梯度优化
  □ render_intensity 从分段线性替换为 sigmoid 叠加（~10 行代码）
    - 打通从人类评分到渲染参数的梯度通路
  □ 用 Phase 3 人类评分数据作为监督信号
  □ 梯度下降优化：source_weight、decay_factor、渲染强度映射曲线
  □ 保持离散等级不变（方案 C：内部连续，外部离散）
  □ 远期方向：clamp → 贝叶斯更新（先验 × 似然 → 后验）

任务 4.5  上下文预测器（梯度下降引入点）
  □ 用 Phase 0-2 积累的数据训练轻量预测器
    - 输入：WSM 状态向量 + 段落嵌入（Sentence-BERT）
    - 输出：下一状态的属性概率分布
    - 架构：小型 MLP（3 层，hidden=256）
    - 训练数据：丰富语料的段落序列（~500 对）
    - 损失函数：交叉熵
  □ 评估：预测准确率 vs 随机基线
  □ 集成：预测器输出作为 PREDICTED 级别证据（置信度上限 0.4）
  □ 对比：有预测器 vs 无预测器的精简语料忠实度
  □ 验证假设："上下文语言的预测性天然带来高忠实度"
  □ 新增训练目标：补全 Parser 的环境信息遗漏（attention 盲区对策）

  ⚡ 里程碑 4：自进化实证 + 预测器验证 + 梯度优化 + 认知对齐
```

---

## 五、关键决策点（Gate）

```
Gate 0 → Phase 1：语料验证通过？信息密度差异显著？
Gate 0.5 → Phase 1：Prompt 稳定性通过？合规率 100%？
Gate 1 → Phase 2：Parser 保真度数据是否支持核心假设？
  - 如果 IEI 泄漏率 > 20% → 回退修改 prompt
  - 如果填充率差异不显著 → 回退检查语料
Gate 2 → Phase 3：渲染质量感知测试通过？
  - 如果不通过 → 调整渲染策略或改为定性研究
Gate 3 → Phase 4：论文核心数据是否足够？
  - 如果 Phase 1-3 数据已足够发表 → Phase 4 作为后续工作
  - 如果需要更强的贡献 → Phase 4 的进化实验作为论文亮点
```

---

## 六、时间线总览

```
Week 1-2    Phase 0     语料验证 + Schema 定义 + 技术选型
Week 2-3    Phase 0.5   Prompt 稳定性验证（3-5 天）
Week 3-5    Phase 1     Parser 管道 + 保真度实验 ← 第一个可发布结果
Week 5-8    Phase 2     渲染 + 自动化评估 + 演示视频
Week 8-11   Phase 3     人类评估 + 论文撰写
Week 12+    Phase 4     进化实验 + 扩展（可选）
```

---

## 七、与进化路径的关系

```
Phase 0-3 证明：人类设计的 LLEE Schema 有效
Phase 4 证明：Schema 可以通过进化算法自动优化
两者合在一起证明：LLEE 不仅是一个静态的工程框架，它是一个可进化的符号系统

这正是 LLEE 区别于 FactTrack / CRANE / ChatUSD 的独特贡献：
  - FactTrack 追踪事实，不进化
  - CRANE 约束解码，不进化
  - ChatUSD 生成 USD，不进化
  - LLEE 的 Schema 本身是进化的对象——这是一个活的语言，不是一个死的格式
```

---

## 八、一句话总结

**LLEE 的实验计划不是在验证一个静态系统，而是在为一个可进化的符号语言建立第一组适应度基准。** Phase 1-3 是基准测量，Phase 4 是进化启动。两者用同一套指标，同一套管道，同一套代码——区别只在于谁在做"选择"：人还是算法。
LLEE不是在“优化一个prompt”，而是在**建立AI生成内容与确定性渲染之间的信任边界**。IEI抑制、证据分级、WSM差分编码——这些具体技术都是为了实现这个边界而做的工程决策。评估数据不是目的，而是证明“这个边界确实被建立起来了”的证据。